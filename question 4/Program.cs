using System;

namespace question_4
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("what card do you have in notations? (eg 2d for two diomands)");
            string userInfo = Console.ReadLine();
            int cardNum = 0;

            switch (userInfo[0])
            {
                case:

                default:
                    break;

            }
        }
    }
}
